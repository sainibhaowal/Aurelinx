:80, :3100, http://localhost:3100 {
    # Route API calls and health checks to the FastAPI backend
    handle /api/* {
        reverse_proxy api:5000
    }

    handle /health {
        reverse_proxy api:5000
    }

    # Route everything else to the Next.js frontend
    handle {
        reverse_proxy web:3000
    }
}
